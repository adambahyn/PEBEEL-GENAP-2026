# Custom Nginx configuration for Railway / Nixpacks
# Increases maximum upload size to 20MB

client_max_body_size 20M;

# Fallback to standard Nixpacks Laravel configuration
location / {
    try_files $uri $uri/ /index.php?$query_string;
}
